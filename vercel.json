{
  "version": 2,
  "builds": [
      {
          "src": "tsnd --transpile-only --respawn --ignore-watch node_modules src/server.ts",
          "use": "@vercel/node"
      }
  ],
  "routes": [
      {
          "src": "src/(.*)",
          "dest": "src/"
      }
  ]
}